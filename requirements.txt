# CFD Studio GUI + pycfd bindings — Python deps (verified on CPython 3.14, Windows).

# Runtime
numpy==2.5.1
matplotlib==3.11.1
PySide6==6.11.1

# Build-time
pybind11==3.0.4

# Packaging (only needed to build the standalone release — see build-release.ps1)
pyinstaller==6.21.0
